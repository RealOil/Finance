# 사용자 피드백 수집

## 현재 요구사항 문서의 관련 내용

### 기존 명시 사항

- 목표: 핵심 기능 검증 및 사용자 피드백 수집 (5.1 Phase 1)
- 사용자 피드백: "이해하기 쉬웠다" 80% 이상 (7.2 사용자 경험 성공)

---

## 추가 확인 필요 사항

### 1. 피드백 수집 방법

**문제 상황**:

- 어떤 방법으로 피드백을 수집할지
- 실시간 vs 비동기
- 익명 vs 실명

**제안**:

#### 옵션 A: 앱 내 피드백 폼 (추천)

**장점**:

- 사용자가 바로 피드백 제공 가능
- 컨텍스트 유지 (어떤 기능 사용 중인지)
- 구현 간단

**구현 방법**:

```python
# 사이드바 또는 하단에 피드백 버튼
st.sidebar.markdown("---")
st.sidebar.markdown("### 💬 피드백")

if st.sidebar.button("피드백 보내기"):
    with st.sidebar.expander("피드백 작성", expanded=True):
        feedback_type = st.radio(
            "피드백 유형",
            ["버그 신고", "기능 제안", "사용성 개선", "기타"]
        )

        feedback_text = st.text_area(
            "내용",
            placeholder="피드백을 입력해주세요..."
        )

        rating = st.slider("만족도", 1, 5, 3)

        if st.button("제출"):
            # 피드백 저장 (예: Google Forms, Email, Database)
            send_feedback(feedback_type, feedback_text, rating)
            st.success("피드백이 전송되었습니다. 감사합니다!")
```

#### 옵션 B: 외부 설문 도구 연동

**Google Forms**:

- 무료
- 간단한 설정
- 자동 데이터 수집

**Typeform**:

- 더 나은 UX
- 유료 플랜 필요

**구현 방법**:

```python
# Google Forms 링크로 리다이렉트
feedback_url = "https://forms.gle/..."

if st.sidebar.button("피드백 보내기"):
    st.markdown(f"""
    <a href="{feedback_url}" target="_blank">
        <button>피드백 작성하기</button>
    </a>
    """, unsafe_allow_html=True)
```

#### 옵션 C: GitHub Issues

**장점**:

- 개발자와 직접 소통
- 버그 추적 용이
- 오픈소스 프로젝트에 적합

**구현 방법**:

```python
# GitHub Issues 링크
github_issues_url = "https://github.com/username/repo/issues/new"

st.sidebar.markdown(f"""
[🐛 버그 신고]({github_issues_url}?template=bug_report.md)
[💡 기능 제안]({github_issues_url}?template=feature_request.md)
""")
```

**권장**: 옵션 A (앱 내 폼) + 옵션 C (GitHub Issues) 조합

---

### 2. 피드백 수집 항목

**문제 상황**:

- 어떤 정보를 수집할지
- 얼마나 자세히 수집할지
- 필수 vs 선택 항목

**제안**:

#### 필수 수집 항목

1. **피드백 유형**: 버그/기능 제안/사용성/기타
2. **내용**: 자유 텍스트
3. **만족도**: 1-5점 척도

#### 선택 수집 항목

4. **사용한 기능**: 어떤 카테고리를 사용했는지
5. **사용 환경**: 브라우저, OS (자동 수집 가능)
6. **연락처**: 추가 질문이 있을 경우 (선택)

#### 구현 예시

```python
def collect_feedback():
    """피드백 수집"""
    feedback_data = {
        'type': feedback_type,
        'content': feedback_text,
        'rating': rating,
        'feature_used': st.session_state.get('current_feature', 'unknown'),
        'timestamp': datetime.now().isoformat(),
        'user_agent': st.session_state.get('user_agent', 'unknown'),
        'contact': contact_email if contact_email else None
    }

    # 저장 (예: JSON 파일, Database, Email)
    save_feedback(feedback_data)
```

---

### 3. 실시간 피드백 vs 설문조사

**문제 상황**:

- 언제 피드백을 요청할지
- 너무 자주 요청하면 사용자 불편
- 적절한 타이밍

**제안**:

#### 실시간 피드백 (즉시)

- **버그 발견 시**: 즉시 피드백 버튼 표시
- **에러 발생 시**: "문제가 발생했습니다. 피드백을 보내주세요"
- **기능 사용 후**: "이 기능이 도움이 되었나요?" (간단한 5점 척도)

#### 설문조사 (주기적)

- **첫 사용 후**: 간단한 온보딩 설문 (선택)
- **주기적**: 월 1회 또는 주요 업데이트 후
- **종료 시**: 앱을 떠날 때 간단한 설문 (선택)

#### 구현 예시

```python
# 기능 사용 후 간단한 피드백
if st.session_state.get('calculation_completed', False):
    st.markdown("---")
    st.markdown("### 이 결과가 도움이 되었나요?")

    col1, col2, col3, col4, col5 = st.columns(5)
    with col1:
        if st.button("😞", key="rating_1"):
            send_quick_feedback(1)
    with col2:
        if st.button("😐", key="rating_2"):
            send_quick_feedback(2)
    with col3:
        if st.button("😊", key="rating_3"):
            send_quick_feedback(3)
    with col4:
        if st.button("😄", key="rating_4"):
            send_quick_feedback(4)
    with col5:
        if st.button("🤩", key="rating_5"):
            send_quick_feedback(5)
```

---

### 4. 피드백 저장 및 분석

**문제 상황**:

- 피드백을 어디에 저장할지
- 어떻게 분석할지
- 우선순위 결정 방법

**제안**:

#### 저장 방법

**옵션 A: JSON 파일 (Phase 1)**

- 간단하고 빠름
- 로컬 또는 GitHub에 저장
- 수동 분석

**옵션 B: Google Sheets (Phase 1-2)**

- Google Forms와 연동
- 자동으로 스프레드시트에 저장
- 간단한 분석 가능

**옵션 C: Database (Phase 2)**

- 구조화된 저장
- 고급 분석 가능
- 대용량 처리

#### 구현 예시 (JSON 파일)

```python
import json
from datetime import datetime

FEEDBACK_FILE = "feedback.json"

def save_feedback(feedback_data):
    """피드백 저장"""
    try:
        # 기존 피드백 로드
        feedbacks = []
        if os.path.exists(FEEDBACK_FILE):
            with open(FEEDBACK_FILE, 'r', encoding='utf-8') as f:
                feedbacks = json.load(f)

        # 새 피드백 추가
        feedbacks.append(feedback_data)

        # 저장
        with open(FEEDBACK_FILE, 'w', encoding='utf-8') as f:
            json.dump(feedbacks, f, ensure_ascii=False, indent=2)

        return True
    except Exception as e:
        st.error(f"피드백 저장 실패: {str(e)}")
        return False
```

---

### 5. 피드백 응답 및 개선

**문제 상황**:

- 피드백을 받은 후 어떻게 처리할지
- 사용자에게 피드백 반영 여부 알림
- 개선 사항 추적

**제안**:

#### 피드백 처리 프로세스

1. **수집**: 피드백 수집
2. **분류**: 버그/기능 제안/사용성으로 분류
3. **우선순위**: 중요도와 구현 난이도 평가
4. **개발**: 우선순위 높은 항목부터 구현
5. **공지**: 개선 사항 공지 (선택)

#### 공지 방법

- **GitHub Releases**: 버전별 개선 사항
- **앱 내 공지**: 주요 개선 사항 표시
- **이메일**: 중요 업데이트 시 (선택)

---

## 구현 우선순위

### Phase 1 (필수)

1. ✅ 간단한 피드백 폼 (앱 내)
2. ✅ GitHub Issues 링크
3. ✅ JSON 파일 저장
4. ✅ 기본 피드백 항목 (유형, 내용, 만족도)

### Phase 2 (권장)

5. ⚠️ Google Forms 연동
6. ⚠️ 실시간 피드백 (기능 사용 후)
7. ⚠️ 피드백 분석 대시보드
8. ⚠️ 자동 응답 시스템

---

## 사용자 경험 원칙

1. **선택적**: 피드백은 항상 선택 사항
2. **간단함**: 최소한의 입력으로 피드백 가능
3. **명확함**: 피드백이 어떻게 사용되는지 명시
4. **감사**: 피드백 제공 시 감사 메시지
